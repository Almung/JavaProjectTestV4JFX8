public class Main {
    public static void main(String[] args) {
        // Initialisation du système
        /*PropertyManager model = new PropertyManager();
        PropertyView view = new PropertyView();
        PropertyController controller = new PropertyController(model, view);

        // Démarrage de l'application
        System.out.println("=== Système de Gestion Immobilière ===");
        System.out.println("Bienvenue dans votre application de gestion immobilière");

        // Lancement du contrôleur
        controller.run();*/
    }
}